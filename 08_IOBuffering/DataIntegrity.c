#include <stdio.h>

int main()
{
    /*
        1). fsync() forces the file to the syn-chronized I/O file integrity completion state.
        2). The fdatasync() system call operates similarly to fsync(), but only forces the file
        to the synchronized I/O data integrity completion state
    */
    return 0;
}
